package com.tom.rv2ide.plugins;

import org.gradle.api.Plugin;
import org.gradle.api.Project;

public class KotlincArtPlugin implements Plugin<Project> {
    @Override
    public void apply(Project target) {
        // No-op stub: plugin exists so the id resolves.
        // AsmClassVisitorFactory passes will be added here when needed.
    }
}
